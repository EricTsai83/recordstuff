原生工具：mcp__cua_repl.js
cua.getState() 成功；文件提供 Target.pressKey(key)，支援 xdotool-style 組合鍵。
依序取得原生 App 物件的結果：
cua.getApp("com.google.Chrome") → Computer Use was not approved to use Google Chrome
cua.getApp("com.apple.Safari") → Computer Use was not approved to use Safari
cua.getApp("com.apple.finder") → Computer Use was not approved to use Finder
cua.getApp("com.apple.QuickTimePlayerX") → Computer Use was not approved to use QuickTime Player
沒有取得可用 App 物件，因此沒有呼叫 pressKey、沒有開始或停止錄影、沒有開啟素材分頁。
拒絕未附更詳細原因；未繞過核准，未使用 AppleScript／osascript 或其他代按方式。
初始 inventory 顯示 Chrome、Finder、QuickTime Player 已在執行；未變更其原有視窗或結束使用者原有 App。

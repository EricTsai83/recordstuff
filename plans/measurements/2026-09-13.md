# 量測 2026-09-13

由 `pnpm verify`／`pnpm matrix` 產生（plans/008）。門檻表見計畫檔；「主觀比對」由人填。

## 先看這裡：怎麼讀這個檔（Claude，2026-09-13 結案時補記）

這個檔是時間順序的流水帳，**只有最後一節 `pnpm matrix -- all`（03:34–03:41）是有效量測**；前面 02:26 quick（素材頁開錯螢幕）與 03:18 levels（1080p 錄成 1080x606 的 bug 未修）的叉叉是當時真的有問題，留著當紀錄。

`all` 那一節每段仍有三列 ❌，各自的意思：

| 列 | 為什麼是 ❌ | 算不算問題 |
|---|---|---|
| 音訊−影像偏移 56–92 ms | 聲音固定比畫面晚約 50–80 ms（扣偵測器偏差），超過當時借自 Cap 的 50 ms 門檻 | 真實現象、每段穩定。之後門檻改採 ITU-R BT.1359（晚 < 125 ms 察覺不到），這列以新門檻是 ✅，決定不補償 |
| 取樣率／聲道 1 聲道 | 跑這輪時還沒加 `channelCount: 2` | 之後已加，現在錄會是 2 聲道（但左右內容相同） |
| 音訊位元率 25–42 kbps | 素材頁只有每秒一聲 beep，其餘靜音，AAC 對靜音只用很少位元 | 素材造成，不是錯；有聲音時實測約 160 kbps，不論要求 192 或 256 k，因此之後移除了音訊品質選單 |

另外 60 fps 那段：幀率 57.3（門檻 58–62 差一點）、位元率為目標兩倍（Chromium 行為）。

**結論**：影像全部通過（尺寸、幀率、掉幀、位元率 99–100%、CPU 15%、3 分鐘漂移 3 ms）。聲音有兩個已接受的事實：固定晚約 60 ms（在察覺門檻內，不補償）、內容單聲道（dual-mono）。「結果：❌」那行是因為工具規則是任一列 ❌ 就 ❌，讀的時候對照上表。

- 機器：Apple M1 Pro，32 GB
- OS：darwin 25.5.0（macOS 26.5.2）
- Electron：44.3.0；Node 24.21.0
- 螢幕：Resolution: 1920 x 1080 (1080p FHD - Full High Definition)；UI Looks like: 1920 x 1080 @ 60.00Hz；Main Display: Yes；Resolution: 1080 x 1920；UI Looks like: 1080 x 1920 @ 60.00Hz
- ffmpeg：ffmpeg version 9.0.1 Copyright (c) 2000-2026 the FFmpeg developers

## 2026-09-12T18:28:37.614Z — pnpm matrix -- quick

### 1440p 標準（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 02-26-31.mp4`
- 要求與 track：影像 standard，上限 1440p，30 fps，音訊 high；track 1440x1440 @ 30 fps，48000 Hz × 1 聲道；目標 8.10 Mbps / 256 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1440x1440 | 1440x810（與 track size 1440x1440 不同） | ❌ |
| 平均幀率 | 30 ± 2 fps | 29.43 fps（883 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.11%（1 張／取樣 883 張，最大間隔 59 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -14 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | —（需 --sync 與測試素材頁） | — |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -26.7 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.10 Mbps ± 30% | 2.50 Mbps（目標的 31%）（超出即為 Chromium 實際夾住的值） | ❌ |
| 音訊位元率 | 256 kbps | 38 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 0%，峰值 0% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，9.1 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1440p 高品質（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 02-27-17.mp4`
- 要求與 track：影像 high，上限 1440p，30 fps，音訊 high；track 1440x1440 @ 30 fps，48000 Hz × 1 聲道；目標 14.90 Mbps / 256 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1440x1440 | 1440x810（與 track size 1440x1440 不同） | ❌ |
| 平均幀率 | 30 ± 2 fps | 29.30 fps（879 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.11%（1 張／取樣 879 張，最大間隔 64 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -5 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | —（需 --sync 與測試素材頁） | — |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -19.4 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 14.90 Mbps ± 30% | 1.21 Mbps（目標的 8%）（超出即為 Chromium 實際夾住的值） | ❌ |
| 音訊位元率 | 256 kbps | 47 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 0%，峰值 0% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，4.5 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 原尺寸 標準 音訊 192k（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 02-28-02.mp4`
- 要求與 track：影像 standard，上限 source，30 fps，音訊 standard；track 1920x1920 @ 30 fps，48000 Hz × 1 聲道；目標 14.40 Mbps / 192 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1920 | 1920x1080（與 track size 1920x1920 不同） | ❌ |
| 平均幀率 | 30 ± 2 fps | 29.27 fps（878 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.11%（1 張／取樣 878 張，最大間隔 68 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -6 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | —（需 --sync 與測試素材頁） | — |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -16.6 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 14.40 Mbps ± 30% | 1.47 Mbps（目標的 10%）（超出即為 Chromium 實際夾住的值） | ❌ |
| 音訊位元率 | 192 kbps | 33 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 0%，峰值 0% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，5.4 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：


## 2026-09-13 02:26 quick 這一輪的備註（Claude，人工補記）

- 這一輪是工具的第一次端對端試跑，**不是有效的品質量測**：`--open-material` 把 Chrome kiosk 開到了第二個螢幕，主螢幕（被錄的那個）當時停在一個影片播放器的靜態畫面，所以三段的影像位元率只有目標的 8–31%（靜態內容，編碼器沒用滿目標），閃光／短音也偵測不到（beep 有錄到、閃光不在畫面裡）。runner 已改為 `--window-position=0,0` 讓 kiosk 固定落在主螢幕。
- CPU 全為 0% 是 runner 的 bug：`pgrep` 用的是 `node_modules/electron` 的 symlink 路徑，實際程序命令列是 pnpm 的 `.pnpm/electron@44.3.0/…` 實體路徑。已改用 `realpath` 比對；下一輪會有數字。
- 即使畫面不對，這一輪仍答了幾件事（與 2026-09-13 01-30-15 的手動錄製一致）：
  - **成品尺寸 ≠ `track size=`**：要求 1440p 上限時 `getSettings()` 回報 1440x1440，實際成品是 1440x810（比例正確、在上限內）；原尺寸那段也回報 1920x1920 而成品是 1920x1080，所以不只是 `applyConstraints` 的約束值，`getSettings()` 的 height 本身就不可信（疑似取到兩個螢幕中的較大邊）。`capture:` log 的 `track size=` 在套用上限後不可信，應改讀實際影格尺寸（交 007 回填／後續修正）。
  - **系統音訊是單聲道**：`channels=1`，成品 1 聲道，不論音訊品質設定。這是 macOS loopback（CoreAudio Tap）給的 track，非編碼設定所致；門檻表「2 聲道、左右皆有能量」目前必定 ❌，要決定是接受單聲道還是要求 `channelCount: 2`。
  - **音訊位元率遠低於目標**（33–47 kbps vs 192／256 kbps）：內容幾乎是靜音（只有每秒的 beep），AAC 編碼器在靜音時用很少位元；01-30-15 那段有音樂時是 160 kbps（目標 256 k），仍低於目標，可能是單聲道時 Chromium 夾住的值。需要用有連續音樂的素材再量。
  - 幀率 29.3–29.4 fps、掉幀 0.11%、容器內起始偏移 0 ms、時長差 −14～−5 ms，皆在門檻內。
- 音畫偏移偵測器用 ffmpeg 合成檔（黑底、右上角每秒 100 ms 白閃、每秒 60 ms 1 kHz beep、音訊人為延遲 30 ms）驗證：量到 39 ms（review 後排除 EOF 收尾：7 對、41 ms），偵測器本身約有 +10 ms 的偏差（blackdetect／silencedetect 的門檻與 AAC 起始延遲），在 50 ms 門檻下可接受，判讀時要記得。
## 2026-09-12T19:20:39.443Z — pnpm matrix -- levels

### 1080p 精省（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-18-31.mp4`
- 要求與 track：影像 economy，上限 1080p，30 fps，音訊 high；track 1080x1080 @ 30 fps，48000 Hz × 1 聲道；目標 2.40 Mbps / 256 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1080x1080 | 1080x606（與 track size 1080x1080 不同） | ❌ |
| 錄製時長 | 30 ± 2 s | 30.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.36 fps（881 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.23%（2 張／取樣 881 張，最大間隔 58 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -9 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 71 ms（27 對；頭 71 ms，尾 71 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -16.6 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 2.40 Mbps ± 30% | 2.35 Mbps（目標的 98%） | ✅ |
| 音訊位元率 | 256 kbps | 44 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 15%，峰值 19% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，8.6 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1080p 標準（原 8 Mbps 基準）（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-19-17.mp4`
- 要求與 track：影像 standard，上限 1080p，30 fps，音訊 high；track 1080x1080 @ 30 fps，48000 Hz × 1 聲道；目標 4.50 Mbps / 256 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1080x1080 | 1080x606（與 track size 1080x1080 不同） | ❌ |
| 錄製時長 | 30 ± 2 s | 30.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.34 fps（881 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 881 張，最大間隔 43 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -32 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 60 ms（30 對；頭 60 ms，尾 60 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -19.3 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 4.50 Mbps ± 30% | 4.42 Mbps（目標的 98%） | ✅ |
| 音訊位元率 | 256 kbps | 44 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 15%，峰值 19% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，16.0 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1080p 高品質（30 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-20-04.mp4`
- 要求與 track：影像 high，上限 1080p，30 fps，音訊 high；track 1080x1080 @ 30 fps，48000 Hz × 1 聲道；目標 8.40 Mbps / 256 kbps
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1080x1080 | 1080x606（與 track size 1080x1080 不同） | ❌ |
| 錄製時長 | 30 ± 2 s | 30.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.29 fps（879 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 879 張，最大間隔 48 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -18 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 68 ms（29 對；頭 68 ms，尾 68 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -19.4 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.40 Mbps ± 30% | 8.22 Mbps（目標的 98%） | ✅ |
| 音訊位元率 | 256 kbps | 43 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 15%，峰值 20% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，30.0 s，29.6 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：


## 2026-09-13 03:18 levels 這一輪的備註（Claude，人工補記）

- 這輪是第一筆**有效**量測：素材頁在主螢幕、同步標記偵測到、CPU 有數字。工具流程沒有問題。
- 但三段成品都是 **1080x606**，不是 1920x1080。原因是 `getSettings()` 把主螢幕回報成 1920x1920，`fitWithinCap` 算出 1080x1080 的約束，Chromium 保持比例輸出 1080x606；位元率也照假尺寸算（標準等級 4.5 Mbps 而非 8.1）。capture host 已改從實際影格量尺寸（見 008 執行紀錄），這三段的尺寸與位元率數字**不代表修正後的行為**，要重跑。
- 仍可用的結論：CPU 1080p30 三等級皆平均 15%／峰值 19–20%；幀率 29.3 fps；掉幀 0–0.23%；位元率在動態素材下達目標的 98%（所以位元率公式本身被 Chromium 尊重）；音畫偏移穩定 60–71 ms（音訊晚），扣偵測器偏差約 50–60 ms，屬固有延遲。
## 2026-09-12T19:41:58.458Z — pnpm matrix -- all

### 1080p 精省（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-34-39.mp4`
- 要求與 track：影像 economy，上限 1080p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 4.40 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.36 fps（440 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 440 張，最大間隔 42 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | 8 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 56 ms（14 對；頭 56 ms，尾 56 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -23.8 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 4.40 Mbps ± 30% | 4.40 Mbps（目標的 100%） | ✅ |
| 音訊位元率 | 256 kbps | 37 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 15%，峰值 17% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，7.9 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1080p 標準（原 8 Mbps 基準）（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-35-11.mp4`
- 要求與 track：影像 standard，上限 1080p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 8.10 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.37 fps（441 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 441 張，最大間隔 39 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -17 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 69 ms（14 對；頭 69 ms，尾 69 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -16.8 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.10 Mbps ± 30% | 8.05 Mbps（目標的 99%） | ✅ |
| 音訊位元率 | 256 kbps | 41 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 14%，峰值 16% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，14.5 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1080p 高品質（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-35-42.mp4`
- 要求與 track：影像 high，上限 1080p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 14.90 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.31 fps（440 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 440 張，最大間隔 42 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -14 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 71 ms（15 對；頭 71 ms，尾 71 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -25.1 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 14.90 Mbps ± 30% | 14.74 Mbps（目標的 99%） | ✅ |
| 音訊位元率 | 256 kbps | 39 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 16%，峰值 19% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，26.5 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 原尺寸 標準 60 fps（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-36-15.mp4`
- 要求與 track：影像 standard，上限 source，60 fps，音訊 high；track 1920x1080 @ 60 fps，48000 Hz × 1 聲道；目標 16.20 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 60 ± 2 fps | 57.33 fps（859 張）（素材需持續動態；靜態畫面不適用） | ❌ |
| 掉幀 | < 2% | 0.23%（2 張／取樣 859 張，最大間隔 39 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | 14 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 86 ms（15 對；頭 86 ms，尾 86 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -16.6 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 16.20 Mbps ± 30% | 31.20 Mbps（目標的 193%）（超出即為 Chromium 實際夾住的值） | ❌ |
| 音訊位元率 | 256 kbps | 41 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 23%，峰值 24% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，55.8 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1440p 標準（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-36-51.mp4`
- 要求與 track：影像 standard，上限 1440p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 8.10 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.33 fps（439 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 439 張，最大間隔 40 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | 27 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 92 ms（15 對；頭 92 ms，尾 92 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -16.8 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.10 Mbps ± 30% | 8.05 Mbps（目標的 99%） | ✅ |
| 音訊位元率 | 256 kbps | 42 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 16%，峰值 19% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，14.4 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1440p 高品質（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-37-23.mp4`
- 要求與 track：影像 high，上限 1440p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 14.90 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.35 fps（440 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 440 張，最大間隔 45 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | 4 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 59 ms（15 對；頭 59 ms，尾 59 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -19.2 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 14.90 Mbps ± 30% | 14.75 Mbps（目標的 99%） | ✅ |
| 音訊位元率 | 256 kbps | 41 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 16%，峰值 17% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，26.4 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 原尺寸 標準 音訊 192k（15 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-37-56.mp4`
- 要求與 track：影像 standard，上限 source，30 fps，音訊 standard；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 8.10 Mbps / 192 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 15 ± 2 s | 15.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.34 fps（440 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 440 張，最大間隔 40 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -1 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 59 ms（15 對；頭 59 ms，尾 59 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | —（需 --sync 且檔案長於 60 秒） | — |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -25.1 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.10 Mbps ± 30% | 8.03 Mbps（目標的 99%） | ✅ |
| 音訊位元率 | 192 kbps | 25 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 16%，峰值 18% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，15.0 s，14.4 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：

### 1080p 標準 30 fps 3 分鐘（漂移）（180 s）

- 檔案：`/Users/eric/Movies/RecordStuff/2026-09-13 03-38-28.mp4`
- 要求與 track：影像 standard，上限 1080p，30 fps，音訊 high；track 1920x1080 @ 30 fps，48000 Hz × 1 聲道；目標 8.10 Mbps / 256 kbps；warnings: track.getSettings() 回報 1920x1920，實際影格 1920x1080，以實際影格為準
- 素材：scripts/test-material.html（Chrome kiosk）

| 指標 | 門檻／要求 | 實測 | 判定 |
|---|---|---|---|
| 成品尺寸 | 1920x1080 | 1920x1080 | ✅ |
| 錄製時長 | 180 ± 2 s | 180.0 s | ✅ |
| 平均幀率 | 30 ± 2 fps | 29.28 fps（5270 張）（素材需持續動態；靜態畫面不適用） | ✅ |
| 掉幀 | < 2% | 0.00%（0 張／取樣 3597 張，最大間隔 47 ms） | ✅ |
| 音訊−影像時長差 | < 100 ms | -26 ms | ✅ |
| 音訊−影像起始偏移（容器） | < 50 ms | 0 ms | ✅ |
| 音訊−影像偏移（閃光／短音） | < 50 ms；穩定超過記為固有延遲 | 70 ms（179 對；頭 71 ms，尾 73 ms） | ❌ |
| 結尾音畫漂移 | < 100 ms | 3 ms | ✅ |
| 取樣率／聲道 | 48 kHz、2 聲道、左右皆有能量 | 48000 Hz、1 聲道，RMS -27.8 dB（track 回報 1 聲道） | ❌ |
| 影像位元率 | 8.10 Mbps ± 30% | 7.93 Mbps（目標的 98%） | ✅ |
| 音訊位元率 | 256 kbps | 42 kbps | ❌ |
| CPU（Electron 各程序合計） | 記錄；門檻待第一次量測後定 | 平均 16%，峰值 21% | — |
| 檔案可播（ffprobe 解碼全部影格） | 無解碼錯誤 | 可解碼，180.0 s，171.1 MB（QuickTime／Chrome 雙擊與拖曳仍需人工） | ✅ |

結果：❌

主觀比對（人填）：

- 文字清晰度：
- 捲動與動態：
- 色彩邊緣（紅藍細線）：
- 音量／失真／左右聲道：


## 2026-09-13 03:34 all 這一輪的備註與決定（Claude，人工補記）

- 第一筆完整有效量測：九段皆成功、素材頁在主螢幕、尺寸修正生效（全部 1920x1080）。決定與結論整理在 `plans/008-recording-verification-toolkit.md` 執行紀錄「第三輪」表格：係數維持、60 fps 開放（位元率約兩倍目標）、CPU 門檻 ≤ 40%、固有延遲 45–80 ms、3 分鐘漂移 3 ms。
- 之後另做兩個 5–7 秒實錄（不在矩陣內）：加 `channelCount: { ideal: 2 }` 後 track 與成品為 2 聲道；用「左聲道 3 秒 440 Hz → 右聲道 3 秒」的測試音（`afplay`）錄製，兩個聲道在每一時段的 RMS 完全相同（−39.2 / −39.2；−36.9 / −36.9 dB），即 **dual-mono**：Electron 44 的 macOS loopback 沒有保留左右分離。成品 AAC 位元率 167 kbps（要求 256 k）。
- 「主觀比對」欄位仍待使用者填（並排看四個等級／聽聲音）；QuickTime 雙擊與拖曳待人工。

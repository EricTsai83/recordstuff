# macOS 0.1.1 release verification

[English](0.1.1.md) | [繁體中文](../../zh-TW/verification/releases/0.1.1.md)

2026-09-15: 011 in progress. The existing identity has been securely provisioned into release environment secrets; no identity rotation. CI, draft and local API download checks have passed. Manual recording/playback/language acceptance, public promotion and public browser download verification remain pending.

First [CI 34878515175](https://github.com/EricTsai83/recordstuff/actions/runs/34878515175) stopped at pnpm check: the new --verify-app test did not set subprocess cwd to its fixture, accidentally depending on local dist. Fixed cwd to the fixture project without weakening artifact checks. Signing and draft creation did not run; failed checks prevented delivery.

Second [CI 34878724150](https://github.com/EricTsai83/recordstuff/actions/runs/34878724150) passed code checks and imported the original fingerprint, but packaging stopped because Electron 44 has no postinstall and a clean installation lacks its runtime. Added explicit official install.js execution. Failure cleanup also stalled; each cleanup operation now has a 15-second timeout, warning and temporary-file removal. OS cleanup failures still rely on disposable runner teardown; persistent runners are unsupported. This run was cancelled without a draft.

The old always-cleanup step remained stuck after cancellation and held concurrency. Queued run 34879342232 was cancelled before starting; the fixed workflow uses the new constant group recordstuff-macos-delivery. The old run had failed packaging without uploading an artifact, so it cannot create a draft. New releases remain serialized by one constant group.

## Successful candidate

[CI 34879680630](https://github.com/EricTsai83/recordstuff/actions/runs/34879680630) completed build and draft jobs. Source and tag resolve to `e98d2feb04b7975a90cf25193f108fbefdf04f1e`, using macOS 15 arm64, Node 24.21.0 and pnpm 10.33.4. CI passed typecheck/build and 17 test files: 262 passed, one conditional FFmpeg/ffprobe integration test skipped because those tools were unavailable. Full local pnpm check passed 263 tests; after the fixture fix, all 26 signing tests passed again.

All nine bundle identities, dates, identifiers, runtime flags and designated requirement passed. DMG integrity, arm64/version, guide bytes, Applications link and visible root contents passed. The independent draft job reverified without private keys. The draft contains three assets: DMG, SHA256SUMS and release.json, with English notes linking bilingual guides and disclosing the Finder limitation.

- File: `RecordStuff-0.1.1-arm64-selfsigned.dmg`
- Size: 127,310,755 bytes
- SHA-256: `be8119c6405c21a5b19f7cab968af5e488435412c8bbc1fc0c663550d40bb512`
- [Source metadata](0.1.1.json)

Local GitHub artifact downloading did not complete and was stopped. `gh release download` successfully downloaded all draft assets to `/tmp/recordstuff-plan011/candidate`. Local release.mts verify passed read-only mounting and artifact checks; GitHub asset API digests match local bytes. Actual CLI negative checks rejected a mismatched version, a reused draft version, missing manual_acceptance, and altered SHA256SUMS. The local checksum file was restored afterward; remote assets were unchanged.

Runner security remove-trusted-cert timed out and emitted a warning. Subsequent keychain deletion succeeded and certificate/PKCS#12 temporary files were removed. Disposable runner teardown removes remaining trust state; persistent runners are unsupported. Actions v4 emitted Node 20 deprecation warnings; GitHub ran them on Node 24 and the jobs succeeded. Future action upgrades require repinning SHAs.

Chrome automation following the draft DMG link hit ERR_BLOCKED_BY_CLIENT after redirect; no browser security settings were changed. The user was asked to download normally, replace/install and verify recording/audio/language persistence/quit/relaunch. No result has been received; installation and publication are not claimed. API downloading is not browser verification.

# 003 第一次有聲音的真實錄製

狀態：進行中（基本有聲有影已確認；完整驗收改用 008 的工具補齊）
前置：007（設定與 `capture:` log）、008（`pnpm verify`／`pnpm matrix`、驗收門檻表；已完成）
後續：004

## 目標

用 `pnpm start` 在這台 Mac 錄出第一個有畫面、有聲音、長度正確、QuickTime 雙擊能播的 MP4。這是 001 里程碑 1 的核心交付；使用者已確認停止後有聲有影；目前尚無完整時長、播放器、同步與效能紀錄。使用 007 的設定與 008 的工具補齊驗收，不必重查已排除的缺音訊權限原因。步驟 5、6、9 的量測改跑 `pnpm matrix -- long` 與 `pnpm verify -- <mp4> --sync --screen <主螢幕尺寸>`，門檻以 008 的表為準，結果讀 `plans/measurements/<日期>.md`（含 CPU 平均／峰值、結尾漂移）。步驟 4 的「QuickTime 能播、能拖曳」仍是人工。

## 已確認的進度

2026-09-13 008 的 `pnpm matrix -- all` 已量到：1080p30 三等級尺寸／幀率／掉幀／位元率皆在門檻內、CPU 平均 15%、3 分鐘漂移 3 ms、音畫固有延遲 45–80 ms（音訊晚，超過 50 ms 門檻，每段內穩定）。固有延遲已決定不補償（落在 ITU-R BT.1359 察覺門檻內）。本計畫剩下：主觀比對（填 `plans/measurements/2026-09-13.md` 的「主觀比對」欄）、QuickTime 雙擊與拖曳、`pnpm matrix -- long` 的 10 分鐘、當機測試、`REC` 字樣。


2026-09-12 使用者回報錄完有畫面、有聲音，但品質與來源有差距。這只確認基本擷取成功，不代表下列各項已驗收；品質差異由 007 追蹤。

## 步驟

1. 確認「系統設定 → 隱私權與安全性 → 螢幕與系統音訊錄製」裡，之前誤授權給終端機／其他 app 的「僅系統音訊錄製」已關閉。
2. `pnpm start`，另開終端機 `tail -f` log（002）。
3. 放一段有節拍的音樂或影片。左鍵點圖示；第一次應出現「Electron 想要錄製系統音訊」，允許。若進 `no_audio_track`，看設定頁下方「僅系統音訊錄製」是否有 Electron 並開啟，再點一次。
4. 錄 60 秒，再點一下停止。確認：通知「已儲存 …」、點通知在 Finder 顯示、檔案在 `~/Movies/RecordStuff`、QuickTime 雙擊能播、有聲音、長度約 60 秒、拖曳進度條是否可用。
5. 用 007 的選單指定 1080p 上限、30 fps，確認實際輸出後錄 10 分鐘；記錄影像品質選項。看 Activity Monitor：Electron 各程序 CPU 合計、是否有 `VTEncoderXPCService` 活動（硬體編碼）。記檔案大小。
6. 音畫偏移：畫面放一個節拍器或秒表，錄 10 分鐘，結尾對照聲音與畫面。
7. 當機測試：錄製中用 Activity Monitor 強制結束 capture host 的 renderer（Electron Helper (Renderer)）。確認通知「錄製程序當機，已保留部分錄影」、`.recording.mp4` 留下、QuickTime 與 Chrome 是否能播、duration 是否正確。
8. 錄製中從右鍵選單「結束」：確認檔案完整、改名為 `.mp4`。
9. HiDPI：檢查錄出的解析度是邏輯還是實體像素。
10. 看選單列的 `REC` 字樣有沒有被錄進影片。

## 記錄

結果寫回 001 §17 對應題目（第 1、2、3、5、7、8 題），標「已答」並附數字。若第 1 或第 2 題答案是不可行，依 001 §16 的退路處理，不要硬撐。

## 完成標準

- 步驟 4 全部通過。
- 001 §17 第 1、2、3、5、7、8 題有答案。
